@php $farmName = function_exists('setting') ? (setting('farm.name') ?: setting('app.name')) : config('app.name'); @endphp
<!doctype html><html><head><meta charset="utf-8"><style>
@page{margin:34px}body{font-family:DejaVu Sans,sans-serif;font-size:10px;color:#111827}.header{border-bottom:2px solid #111827;padding-bottom:10px}.title{font-size:20px;font-weight:700}.meta,.amounts{width:100%;border-collapse:collapse;margin-top:14px}.meta td,.amounts td,.amounts th{border:1px solid #d1d5db;padding:7px}.amounts th{background:#f3f4f6}.right{text-align:right}.big{font-size:15px;font-weight:700}.note{margin-top:12px;padding:9px;background:#f9fafb;border:1px solid #e5e7eb}.footer{margin-top:30px;color:#6b7280;font-size:8px}
</style></head><body>
<div class="header"><div class="title">{{ $farmName ?: 'Penzi Farm' }}</div><div><strong>Operating Expense Voucher</strong></div></div>
<table class="meta"><tr><td><strong>Expense No.</strong><br>{{ $expense->expense_number }}</td><td><strong>Date</strong><br>{{ $expense->expense_date?->format('d M Y') }}</td><td><strong>Status</strong><br>{{ $expense->status_label }}</td></tr><tr><td><strong>Category</strong><br>{{ $expense->category?->name }}</td><td><strong>Supplier</strong><br>{{ $expense->supplier?->company_name ?: 'Direct / Other' }}</td><td><strong>Invoice / eTIMS</strong><br>{{ $expense->supplier_invoice_number ?: 'N/A' }} / {{ $expense->etims_invoice_number ?: 'N/A' }}</td></tr></table>
<div class="note"><strong>Description:</strong> {{ $expense->description }}</div>
<table class="amounts"><thead><tr><th>Before VAT</th><th>VAT</th><th>WHT</th><th>Gross</th><th>Supplier Payable</th><th>Balance</th></tr></thead><tbody><tr><td class="right">{{ number_format((float)$expense->net_amount,2) }}</td><td class="right">{{ number_format((float)$expense->vat_amount,2) }}</td><td class="right">{{ number_format((float)$expense->withholding_tax_amount,2) }}</td><td class="right">{{ number_format((float)$expense->gross_amount,2) }}</td><td class="right big">{{ number_format((float)$expense->payable_amount,2) }}</td><td class="right big">{{ number_format((float)$expense->balance_due,2) }}</td></tr></tbody></table>
<div class="footer">GL Account: {{ $expense->category?->account?->code }} · Supplier KRA PIN: {{ $expense->supplier_kra_pin ?: 'N/A' }} · Generated by {{ $generatedBy?->name ?? 'System' }}</div>
</body></html>
